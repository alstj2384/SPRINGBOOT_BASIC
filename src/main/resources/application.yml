
spring:
  jpa:
    defer-datasource-initialization: true
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  h2:
    console:
      enabled: true
  profiles:
    include: auth

jwt:
  issuer: test@naver.com
  secret_key: springboot